class Region:
    def __init__(self, name, region_type, upper_bound, lower_bound):
        self.name = name
        self.region_type = region_type
        self.upper_bound = upper_bound
        self.lower_bound = lower_bound
